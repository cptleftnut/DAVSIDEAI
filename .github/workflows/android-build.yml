name: DAVSIDE AI - Android Build & Release

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build Android (${{ matrix.variant }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        variant: [debug, release]
        
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK & NDK
        uses: android-actions/setup-android@v3
        with:
          ndk-version: r27

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android, armv7-linux-androideabi, i686-linux-android, x86_64-linux-android

      - name: Cache Rust
        uses: Swatinem/rust-cache@v2

      - name: List files for debugging
        run: |
          echo "Dette er filerne som GitHub Actions kan se lige nu:"
          ls -la

      - name: Install dependencies
        working-directory: ./frontend
        run: |
          if [ ! -f "package.json" ]; then
            echo "Fejl: Kunne ikke finde package.json!"
            echo "1. Tjek at filen er committet og pushet til GitHub."
            echo "2. Tjek at 'frontend' er det rigtige mappenavn."
            exit 1
          fi

          if [ -f "package-lock.json" ]; then
            npm ci
          else
            npm install
          fi

      - name: Install Tauri CLI
        run: cargo install tauri-cli --version "^2.0.0" --locked

      - name: Build frontend
        working-directory: ./frontend
        run: npm run build

      - name: Initialiser Android hvis nødvendigt
        working-directory: ./frontend
        run: |
          if [ ! -d "src-tauri/gen/android" ]; then
            cargo tauri android init
          fi

      - name: Build ${{ matrix.variant }} APK
        working-directory: ./frontend
        env:
          XAI_API_KEY: ${{ secrets.XAI_API_KEY }}
        run: |
          if [ "${{ matrix.variant }}" = "release" ]; then
            cargo tauri android build --release
          else
            cargo tauri android build --debug
          fi

      - name: Upload ${{ matrix.variant }} APK
        uses: actions/upload-artifact@v4
        with:
          name: davside-ai-${{ matrix.variant }}-apk
          # Stien er opdateret til at starte med frontend/
          path: frontend/src-tauri/gen/android/app/build/outputs/apk/${{ matrix.variant }}/*.apk
          retention-days: 14
